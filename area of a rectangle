#include<stdio.h>
int main()
{
int l,b,area;
printf("\nenter the length:");
scanf("%d",&l);
printf("\nenter breadth of rectangle:");
scanf("%d",&b);
area=l*b;
printf("area of rectangle=%d",area);
return 0;
}
