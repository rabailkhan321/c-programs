//*write  a program to sum first 10 natural numbers using while loop*//



#include<stdio.h>
int main()
{
    int n=1,sum=0;
    while(n<=10)
    {
        sum=sum+n;
        
        n++;
    }
printf("the sum is %d",sum);
return 0;
}
